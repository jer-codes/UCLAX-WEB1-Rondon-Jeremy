//component
import Meta from "@App/Core/components/Meta";
import Inset from "@App/Layout/inset";

// component
const Home = () => {
    return (
        <main>
            <Inset>
                <Meta pageTitle="Home" />
                <h1>Home</h1>
            </Inset>
        </main>
    );
};

export default Home;
